name: 'Execução da Pipeline de CI'

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '0 6 * * 2,4,6'

jobs:
  inspecao:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: latest

      - name: Instalando dependências
        run: npm install

      - name: Inspeção de Código
        run: npm run lint

  unidade:
    runs-on: ubuntu-latest
    needs: [inspecao]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: latest

      - name: Instalando dependências
        run: npm install

      - name: Rodando testes unitários
        run: npm run test

      - name: Salvando relatórios de testes
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          path: ./mochawesome-report
          name: Relatório de Testes Unitários

    deploy:
    runs-on: ubuntu-latest
    needs: [inspecao,unidade]

    steps:
      - name: Simulação de Deploy
        run: echo 'Deploy sendo realizado...'